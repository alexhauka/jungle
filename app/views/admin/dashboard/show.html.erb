<h1>Admin Dashboard</h1>
<br>
<div class="card w-50">
  <div class="card-body">
    <h2 class="card-title">Products</h5>
    <p class="card-text">The current number of Products is: <%= @products.count %></p>
    <%= link_to 'All Products', [:admin, :products], class: 'btn btn-info' %>
  </div>
</div>
<br>
<div class="card w-50">
  <div class="card-body">
    <h2 class="card-title">Categories</h5>
    <p class="card-text">The current number of Categories is: <%= @categories.count %></p>

  </div>
</div>
